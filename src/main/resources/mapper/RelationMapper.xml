<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huahuo.huahuobook.mapper.RelationMapper">

    <resultMap id="BaseResultMap" type="com.huahuo.huahuobook.pojo.Relation">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="user_id" column="user_id" jdbcType="INTEGER"/>
            <result property="book_id" column="book_id" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,book_id
    </sql>

    <select id="selectRelationByUserId" resultType="com.huahuo.huahuobook.pojo.Relation">
    select * from relation where user_id = #{id}
    </select>
    <select id="listBook_idByUser_id" resultMap="BaseResultMap">
        select book_id
        from relation
        where user_id = #{user_id,jdbcType=NUMERIC}
    </select>


</mapper>
